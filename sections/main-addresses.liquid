<section class='max-w-screen-2xl py-12 px-4 md:px-8 lg:px-12 mx-auto'>
  <h1 class='text-3xl font-bold mb-8'>{{ 'customer.addresses.title' | t }}</h1>

  {%- paginate customer.addresses by 5 -%}
    <div class='mb-8'>
      <button type='button' class='bg-black hover:bg-gray-800 rounded-lg text-white py-3 px-6 transition' data-address-toggle>
        {{ 'customer.addresses.add_an_address' | t }}
      </button>
    </div>

    <div data-address-form class='hidden border border-gray-200 rounded-lg p-6 mb-8'>
      {%- form 'customer_address', customer.new_address, aria-labelledBy: 'AddressNewHeading' -%}
        {%- if form.errors -%}
          <div class='bg-red-50 border border-red-200 rounded-lg p-4 mb-6'>
            <h2 class='text-red-800 font-semibold mb-2'>{{ 'accessibility.error' | t }}</h2>
            <ul class='text-sm text-red-700 list-disc list-inside'>
              {%- for field in form.errors -%}
                <li>
                  {%- if field == 'form' -%}
                    {{ form.errors.messages[field] }}
                  {%- else -%}
                    <a href='#{{ field }}'>
                      {{- form.errors.translated_fields[field] | capitalize }}
                      {{ form.errors.messages[field] -}}
                    </a>
                  {%- endif -%}
                </li>
              {%- endfor -%}
            </ul>
          </div>
        {%- endif -%}
        <h2 id='AddressNewHeading' class='text-2xl font-bold mb-4'>{{ 'customer.addresses.add_an_address' | t }}</h2>
        <div class='grid grid-cols-1 md:grid-cols-2 gap-4 mb-4'>
          <div>
            <label for='AddressFirstNameNew' class='block font-semibold mb-2'>{{ 'customer.addresses.first_name' | t }}</label>
            <input
              type='text'
              id='AddressFirstNameNew'
              name='address[first_name]'
              value='{{ form.first_name }}'
              autocomplete='given-name'
              class='border border-gray-300 focus:ring-2 focus:ring-black rounded w-full py-2 px-4 focus:outline-none'
              {% if form.errors contains 'first_name' %}
                aria-invalid='true'
                aria-describedby='first_name-error'
              {% endif %}
            >
            {%- if form.errors contains 'first_name' -%}
              <p id='first_name-error' class='text-sm text-red-600 mt-1'>{{ form.errors.messages.first_name }}</p>
            {%- endif -%}
          </div>
          <div>
            <label for='AddressLastNameNew' class='block font-semibold mb-2'>{{ 'customer.addresses.last_name' | t }}</label>
            <input
              type='text'
              id='AddressLastNameNew'
              name='address[last_name]'
              value='{{ form.last_name }}'
              class='border border-gray-300 rounded w-full py-2 px-4'
            >
          </div>
        </div>
        <div class='mb-4'>
          <label for='AddressCompanyNew' class='block font-semibold mb-2'>{{ 'customer.addresses.company' | t }}</label>
          <input
            type='text'
            id='AddressCompanyNew'
            name='address[company]'
            value='{{ form.company }}'
            class='border border-gray-300 rounded w-full py-2 px-4'
          >
        </div>
        <div class='mb-4'>
          <label for='AddressAddress1New' class='block font-semibold mb-2'>{{ 'customer.addresses.address1' | t }}</label>
          <input
            type='text'
            id='AddressAddress1New'
            name='address[address1]'
            value='{{ form.address1 }}'
            class='border border-gray-300 rounded w-full py-2 px-4'
          >
        </div>
        <div class='mb-4'>
          <label for='AddressAddress2New' class='block font-semibold mb-2'>{{ 'customer.addresses.address2' | t }}</label>
          <input
            type='text'
            id='AddressAddress2New'
            name='address[address2]'
            value='{{ form.address2 }}'
            class='border border-gray-300 rounded w-full py-2 px-4'
          >
        </div>
        <div class='grid grid-cols-1 md:grid-cols-3 gap-4 mb-4'>
          <div>
            <label for='AddressCityNew' class='block font-semibold mb-2'>{{ 'customer.addresses.city' | t }}</label>
            <input
              type='text'
              id='AddressCityNew'
              name='address[city]'
              value='{{ form.city }}'
              class='border border-gray-300 rounded w-full py-2 px-4'
            >
          </div>
          <div>
            <label for='AddressCountryNew' class='block font-semibold mb-2'>{{ 'customer.addresses.country' | t }}</label>
            <select
              id='AddressCountryNew'
              name='address[country]'
              data-default='{{ form.country }}'
              class='border border-gray-300 rounded w-full py-2 px-4'
            >
              {{ all_country_option_tags }}
            </select>
          </div>
          <div id='AddressProvinceContainerNew'>
            <label for='AddressProvinceNew' class='block font-semibold mb-2'>{{ 'customer.addresses.province' | t }}</label>
            <select
              id='AddressProvinceNew'
              name='address[province]'
              data-default='{{ form.province }}'
              class='border border-gray-300 rounded w-full py-2 px-4'
            ></select>
          </div>
        </div>
        <div class='grid grid-cols-1 md:grid-cols-2 gap-4 mb-4'>
          <div>
            <label for='AddressZipNew' class='block font-semibold mb-2'>{{ 'customer.addresses.zip' | t }}</label>
            <input
              type='text'
              id='AddressZipNew'
              name='address[zip]'
              value='{{ form.zip }}'
              class='border border-gray-300 rounded w-full py-2 px-4'
            >
          </div>
          <div>
            <label for='AddressPhoneNew' class='block font-semibold mb-2'>{{ 'customer.addresses.phone' | t }}</label>
            <input
              type='tel'
              id='AddressPhoneNew'
              name='address[phone]'
              value='{{ form.phone }}'
              class='border border-gray-300 rounded w-full py-2 px-4'
            >
          </div>
        </div>
        <div class='mb-4'>
          <label class='flex items-center'>
            <input type='checkbox' name='address[default]' value='1' class='mr-2'>
            <span>{{ 'customer.addresses.set_default' | t }}</span>
          </label>
        </div>
        <div class='flex gap-4'>
          <button type='submit' class='bg-black hover:bg-gray-800 rounded-lg text-white py-3 px-6 transition'>
            {{ 'customer.addresses.save' | t }}
          </button>
          <button type='button' class='hover:bg-gray-100 border border-black rounded-lg py-3 px-6 transition' data-address-toggle>
            {{ 'customer.login_page.cancel' | t }}
          </button>
        </div>
      {%- endform -%}
    </div>

    <div class='space-y-6'>
      {%- for address in customer.addresses -%}
        <div data-address class='border border-gray-200 rounded-lg p-6'>
          {%- if address == customer.default_address -%}
            <p class='text-sm font-semibold mb-2'>{{ 'customer.addresses.default' | t }}</p>
          {%- endif -%}
          <div class='mb-4'>
            {{ address | format_address }}
          </div>
          <div class='flex gap-4'>
            <button type='button' class='text-sm underline' data-address-toggle data-form-id='{{ address.id }}'>
              {{ 'customer.addresses.edit' | t }}
            </button>
            <button
              type='button'
              class='text-sm text-red-600 underline'
              data-address-delete
              data-form-url='{{ address.url }}'
              data-confirm-message='{{ 'customer.addresses.delete_confirm' | t }}'
            >
              {{ 'customer.addresses.delete' | t }}
            </button>
          </div>

          <div data-address-forms data-form-id='{{ address.id }}' class='hidden bg-gray-50 border border-gray-200 rounded-lg p-6 mt-4'>
            {%- form 'customer_address', address -%}
              {%- if form.errors -%}
                <div class='bg-red-50 border border-red-200 rounded-lg p-4 mb-6'>
                  <h2 class='text-red-800 font-semibold mb-2'>{{ 'accessibility.error' | t }}</h2>
                  <ul class='text-sm text-red-700 list-disc list-inside'>
                    {%- for field in form.errors -%}
                      <li>
                        {%- if field == 'form' -%}
                          {{ form.errors.messages[field] }}
                        {%- else -%}
                          <a href='#{{ field }}'>
                            {{- form.errors.translated_fields[field] | capitalize }}
                            {{ form.errors.messages[field] -}}
                          </a>
                        {%- endif -%}
                      </li>
                    {%- endfor -%}
                  </ul>
                </div>
              {%- endif -%}
              <h2 class='text-xl font-bold mb-4'>{{ 'customer.addresses.edit_address' | t }}</h2>
              <div class='grid grid-cols-1 md:grid-cols-2 gap-4 mb-4'>
                <div>
                  <label for='AddressFirstName_{{ form.id }}' class='block font-semibold mb-2'>
                    {{- 'customer.addresses.first_name' | t -}}
                  </label>
                  <input
                    type='text'
                    id='AddressFirstName_{{ form.id }}'
                    name='address[first_name]'
                    value='{{ form.first_name }}'
                    class='border border-gray-300 rounded w-full py-2 px-4'
                  >
                </div>
                <div>
                  <label for='AddressLastName_{{ form.id }}' class='block font-semibold mb-2'>
                    {{- 'customer.addresses.last_name' | t -}}
                  </label>
                  <input
                    type='text'
                    id='AddressLastName_{{ form.id }}'
                    name='address[last_name]'
                    value='{{ form.last_name }}'
                    class='border border-gray-300 rounded w-full py-2 px-4'
                  >
                </div>
              </div>
              <div class='mb-4'>
                <label for='AddressCompany_{{ form.id }}' class='block font-semibold mb-2'>{{ 'customer.addresses.company' | t }}</label>
                <input
                  type='text'
                  id='AddressCompany_{{ form.id }}'
                  name='address[company]'
                  value='{{ form.company }}'
                  class='border border-gray-300 rounded w-full py-2 px-4'
                >
              </div>
              <div class='mb-4'>
                <label for='AddressAddress1_{{ form.id }}' class='block font-semibold mb-2'>{{ 'customer.addresses.address1' | t }}</label>
                <input
                  type='text'
                  id='AddressAddress1_{{ form.id }}'
                  name='address[address1]'
                  value='{{ form.address1 }}'
                  class='border border-gray-300 rounded w-full py-2 px-4'
                >
              </div>
              <div class='mb-4'>
                <label for='AddressAddress2_{{ form.id }}' class='block font-semibold mb-2'>{{ 'customer.addresses.address2' | t }}</label>
                <input
                  type='text'
                  id='AddressAddress2_{{ form.id }}'
                  name='address[address2]'
                  value='{{ form.address2 }}'
                  class='border border-gray-300 rounded w-full py-2 px-4'
                >
              </div>
              <div class='grid grid-cols-1 md:grid-cols-3 gap-4 mb-4'>
                <div>
                  <label for='AddressCity_{{ form.id }}' class='block font-semibold mb-2'>{{ 'customer.addresses.city' | t }}</label>
                  <input
                    type='text'
                    id='AddressCity_{{ form.id }}'
                    name='address[city]'
                    value='{{ form.city }}'
                    class='border border-gray-300 rounded w-full py-2 px-4'
                  >
                </div>
                <div>
                  <label for='AddressCountry_{{ form.id }}' class='block font-semibold mb-2'>{{ 'customer.addresses.country' | t }}</label>
                  <select
                    id='AddressCountry_{{ form.id }}'
                    name='address[country]'
                    data-default='{{ form.country }}'
                    class='border border-gray-300 rounded w-full py-2 px-4'
                  >
                    {{ all_country_option_tags }}
                  </select>
                </div>
                <div id='AddressProvinceContainer_{{ form.id }}'>
                  <label for='AddressProvince_{{ form.id }}' class='block font-semibold mb-2'>
                    {{- 'customer.addresses.province' | t -}}
                  </label>
                  <select
                    id='AddressProvince_{{ form.id }}'
                    name='address[province]'
                    data-default='{{ form.province }}'
                    data-form-id='{{ form.id }}'
                    class='border border-gray-300 rounded w-full py-2 px-4'
                  ></select>
                </div>
              </div>
              <div class='grid grid-cols-1 md:grid-cols-2 gap-4 mb-4'>
                <div>
                  <label for='AddressZip_{{ form.id }}' class='block font-semibold mb-2'>{{ 'customer.addresses.zip' | t }}</label>
                  <input
                    type='text'
                    id='AddressZip_{{ form.id }}'
                    name='address[zip]'
                    value='{{ form.zip }}'
                    class='border border-gray-300 rounded w-full py-2 px-4'
                  >
                </div>
                <div>
                  <label for='AddressPhone_{{ form.id }}' class='block font-semibold mb-2'>{{ 'customer.addresses.phone' | t }}</label>
                  <input
                    type='tel'
                    id='AddressPhone_{{ form.id }}'
                    name='address[phone]'
                    value='{{ form.phone }}'
                    class='border border-gray-300 rounded w-full py-2 px-4'
                  >
                </div>
              </div>
              <div class='mb-4'>
                <label class='flex items-center'>
                  <input
                    type='checkbox'
                    name='address[default]'
                    value='1'
                    {% if address == customer.default_address %}
                      checked
                    {% endif %}
                    class='mr-2'
                  >
                  <span>{{ 'customer.addresses.set_default' | t }}</span>
                </label>
              </div>
              <div class='flex gap-4'>
                <button type='submit' class='bg-black hover:bg-gray-800 rounded-lg text-white py-3 px-6 transition'>
                  {{ 'customer.account.update' | t }}
                </button>
                <button
                  type='button'
                  class='hover:bg-gray-100 border border-black rounded-lg py-3 px-6 transition'
                  data-address-toggle
                  data-form-id='{{ address.id }}'
                >
                  {{ 'customer.login_page.cancel' | t }}
                </button>
              </div>
            {%- endform -%}
          </div>
        </div>
      {%- endfor -%}
    </div>

    {%- if paginate.pages > 1 -%}
      <nav role='navigation' aria-label='Pagination' class='mt-8'>
        <ul class='flex justify-center gap-4'>
          {%- if paginate.previous -%}
            <li>
              <a href='{{ paginate.previous.url }}' class='hover:bg-gray-100 border border-gray-300 rounded py-2 px-4'>
                {{ 'accessibility.previous' | t }}
              </a>
            </li>
          {%- endif -%}
          {%- for part in paginate.parts -%}
            <li>
              {%- if part.is_link -%}
                <a href='{{ part.url }}' class='hover:bg-gray-100 border border-gray-300 rounded py-2 px-4'>
                  {{ part.title }}
                </a>
              {%- else -%}
                <span class='bg-gray-100 border border-gray-300 rounded py-2 px-4'>
                  {{ part.title }}
                </span>
              {%- endif -%}
            </li>
          {%- endfor -%}
          {%- if paginate.next -%}
            <li>
              <a href='{{ paginate.next.url }}' class='hover:bg-gray-100 border border-gray-300 rounded py-2 px-4'>
                {{ 'accessibility.next' | t }}
              </a>
            </li>
          {%- endif -%}
        </ul>
      </nav>
    {%- endif -%}
  {%- endpaginate -%}
</section>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    // Handle address form toggles
    document.querySelectorAll('[data-address-toggle]').forEach((button) => {
      button.addEventListener('click', function () {
        const formId = this.getAttribute('data-form-id')
        if (formId) {
          const form = document.querySelector(`[data-address-forms][data-form-id="${formId}"]`)
          if (form) {
            form.classList.toggle('hidden')
          }
        } else {
          const newAddressForm = document.querySelector('[data-address-form]')
          if (newAddressForm) {
            newAddressForm.classList.toggle('hidden')
          }
        }
      })
    })

    // Handle address deletion
    document.querySelectorAll('[data-address-delete]').forEach((button) => {
      button.addEventListener('click', function () {
        if (confirm(this.getAttribute('data-confirm-message'))) {
          const form = document.createElement('form')
          form.method = 'post'
          form.action = this.getAttribute('data-form-url')
          const input = document.createElement('input')
          input.type = 'hidden'
          input.name = '_method'
          input.value = 'delete'
          form.appendChild(input)
          document.body.appendChild(form)
          form.submit()
        }
      })
    })

    // Handle country/province selectors using Shopify's built-in helper
    if (typeof Shopify !== 'undefined' && Shopify.CountryProvinceSelector) {
      // Initialize for new address form
      new Shopify.CountryProvinceSelector('AddressCountryNew', 'AddressProvinceNew', {
        hideElement: 'AddressProvinceContainerNew',
      })

      // Initialize for existing address forms
      document.querySelectorAll('select[id^="AddressCountry_"]').forEach((countrySelect) => {
        const formId = countrySelect.id.replace('AddressCountry_', '')
        const provinceId = `AddressProvince_${formId}`
        const provinceContainerId = `AddressProvinceContainer_${formId}`
        new Shopify.CountryProvinceSelector(countrySelect.id, provinceId, {
          hideElement: provinceContainerId,
        })
      })
    }
  })
</script>

{% schema %}
{
  "name": "t:sections.main-addresses.name",
  "tag": "section"
}
{% endschema %}
